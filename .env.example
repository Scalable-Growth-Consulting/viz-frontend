# Reddit CoPilot API Configuration
# Replace with your actual API Gateway URL
VITE_API_GATEWAY_URL=https://your-api-gateway-id.execute-api.region.amazonaws.com/prod

# Optional: Enable debug logging for API calls
VITE_DEBUG_API=false

# Optional: WebSocket URL for real-time logs (if different from HTTP API)
VITE_WS_URL=wss://your-websocket-api-id.execute-api.region.amazonaws.com/prod
